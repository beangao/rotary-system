WORKLOG.md に作業内容を記録して：

1. `git diff --stat` と `git status` を確認して変更内容を把握
2. WORKLOG.md に今回の作業を追記（日時、作業内容、変更ファイル、コミット）
3. 「最新の状態」セクションを更新（最終作業日、次にやること）
4. `git add WORKLOG.md && git commit -m "docs: 作業履歴更新"` を実行
